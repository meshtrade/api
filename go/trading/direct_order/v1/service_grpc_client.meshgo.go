// Code generated by protoc-gen-meshgo. DO NOT EDIT.
// source: meshtrade/trading/direct_order/v1/service.proto
package directorderv1

import (
	"context"
	"errors"
	fmt "fmt"
	"time"

	"github.com/meshtrade/api/go/common"
	trace "go.opentelemetry.io/otel/trace"
	"go.opentelemetry.io/otel/trace/noop"
	grpc "google.golang.org/grpc"
	"google.golang.org/grpc/credentials"
	"google.golang.org/grpc/credentials/insecure"
	"google.golang.org/grpc/metadata"
)

// DirectOrderServiceGRPCClient is a production-ready gRPC client for the DirectOrderService service.
// It combines the service interface with resource management capabilities, providing
// enterprise-grade features including authentication, timeouts, tracing, and connection pooling.
//
// Features:
//   - Automatic authentication via API key or access token cookies with group ID support
//   - Credentials file loading from MESH_API_CREDENTIALS environment variable
//   - Configurable request timeouts with smart deadline handling
//   - OpenTelemetry distributed tracing support
//   - TLS/mTLS support with configurable transport credentials
//   - Proper resource cleanup with Close() method
//   - Production-ready connection management
//
// Thread Safety:
//
//	This client is safe for concurrent use by multiple goroutines.
//
// Example usage:
//
//	client, err := NewDirectOrderServiceGRPCClient(
//		WithAPIKey("your-api-key"),
//		WithGroup("your-group-id"),
//		WithTimeout(30 * time.Second),
//	)
//	if err != nil {
//		log.Fatal(err)
//	}
//	defer client.Close()
//
//	response, err := client.SomeMethod(context.Background(), &request)
type DirectOrderServiceGRPCClient interface {
	DirectOrderService
	common.GRPCClient
}

// ensure directOrderServiceGRPCClient implements the DirectOrderServiceGRPCClient interface
var _ DirectOrderServiceGRPCClient = &directOrderServiceGRPCClient{}

// directOrderServiceGRPCClient is the internal implementation of the DirectOrderServiceGRPCClient interface.
// This struct maintains the gRPC connection state, authentication credentials,
// and configuration options for the client.
type directOrderServiceGRPCClient struct {
	url                     string
	port                    int
	tls                     bool
	conn                    *grpc.ClientConn
	grpcClient              DirectOrderServiceClient
	tracer                  trace.Tracer
	apiKey                  string
	accessTokenCookie       string
	groupID                 string
	timeout                 time.Duration
	unaryClientInterceptors []grpc.UnaryClientInterceptor
}

// NewDirectOrderServiceGRPCClient creates a new production-ready gRPC client for the DirectOrderService service.
// The client is configured using functional options and automatically handles connection
// management, authentication, timeouts, and distributed tracing.
//
// Default Configuration:
//   - Server: Uses common.DefaultGRPCURL and common.DefaultGRPCPort
//   - TLS: Enabled by default (common.DefaultTLS)
//   - Timeout: 30 seconds for all method calls
//   - Authentication: Attempts to load credentials from MESH_API_CREDENTIALS file
//   - Tracing: Disabled by default (no-op tracer)
//
// Parameters:
//   - opts: Functional options to configure the client (WithAPIKey, WithTimeout, etc.)
//
// Returns:
//   - DirectOrderServiceGRPCClient: Configured client instance
//   - error: Configuration or connection error
//
// Example:
//
//	client, err := NewDirectOrderServiceGRPCClient(
//		WithAPIKey("your-api-key-here"),
//		WithGroup("your-group-id"),
//		WithAddress("api.example.com", 443),
//		WithTimeout(10 * time.Second),
//	)
//	if err != nil {
//		return fmt.Errorf("failed to create client: %w", err)
//	}
//	defer client.Close()
//
// Thread Safety:
//
//	The returned client is safe for concurrent use by multiple goroutines.
func NewDirectOrderServiceGRPCClient(opts ...ClientOption) (DirectOrderServiceGRPCClient, error) {
	// prepare client with default configuration
	client := &directOrderServiceGRPCClient{
		url:     common.DefaultGRPCURL,
		port:    common.DefaultGRPCPort,
		tls:     common.DefaultTLS,
		tracer:  noop.NewTracerProvider().Tracer(""),
		timeout: 30 * time.Second, // default 30 second timeout

		// set once options are applied and connection opened
		grpcClient:              nil,
		unaryClientInterceptors: nil,
	}

	// attempt to load credentials from environment file
	if creds, err := common.CredentialsFromEnvironment(); err == nil {
		client.apiKey = creds.APIKey
		client.groupID = creds.GroupID
	}

	// apply options to the client (these can override credentials from file)
	for _, opt := range opts {
		opt(client)
	}

	// validate authentication credentials
	if err := client.validateAuth(); err != nil {
		return nil, err
	}

	// prepare authentication interceptor
	client.unaryClientInterceptors = []grpc.UnaryClientInterceptor{
		client.authInterceptor(),
	}

	// prepare dial options
	dialOpts := make([]grpc.DialOption, 0)

	// set transport credentials
	if client.tls {
		dialOpts = append(dialOpts, grpc.WithTransportCredentials(credentials.NewClientTLSFromCert(nil, "")))
	} else {
		dialOpts = append(dialOpts, grpc.WithTransportCredentials(insecure.NewCredentials()))
	}

	dialOpts = append(dialOpts, grpc.WithChainUnaryInterceptor(client.unaryClientInterceptors...))

	// construct gRPC client connection
	conn, err := grpc.NewClient(
		fmt.Sprintf("%s:%d", client.url, client.port),
		dialOpts...,
	)
	if err != nil {
		return nil, fmt.Errorf("error constructing grpc client connection: %w", err)
	}

	// set client connection and gRPC client
	client.conn = conn
	client.grpcClient = NewDirectOrderServiceClient(conn)

	// return constructed client
	return client, nil
}

// GetDirectOrder executes the GetDirectOrder RPC method on the DirectOrderService service.
// This method automatically handles authentication, timeouts, distributed tracing, and error propagation.
//
// Timeout Behavior:
//   - If the context already has a deadline, it will be respected
//   - If no deadline is set, the client's configured timeout will be applied
//   - The method will be cancelled if the timeout is exceeded
//
// Authentication:
//   - Automatically includes API key or access token in request headers
//   - Authentication is configured during client creation
//
// Distributed Tracing:
//   - Creates a new span for this method call
//   - Span is automatically finished when the method returns
//   - Errors are recorded in the span
//
// Parameters:
//   - ctx: Context for the request (can include custom timeout, tracing, etc.)
//   - request: The GetDirectOrderRequest containing the method parameters
//
// Returns:
//   - *DirectOrder: The successful response from the service
//   - error: Any error that occurred during the request
//
// Example:
//
//	resp, err := client.GetDirectOrder(ctx, &GetDirectOrderRequest{
//		// populate request fields
//	})
//	if err != nil {
//		return fmt.Errorf("getdirectorder failed: %w", err)
//	}
func (s *directOrderServiceGRPCClient) GetDirectOrder(ctx context.Context, request *GetDirectOrderRequest) (*DirectOrder, error) {
	// apply timeout if no deadline is already set
	if _, hasDeadline := ctx.Deadline(); !hasDeadline {
		var cancel context.CancelFunc
		ctx, cancel = context.WithTimeout(ctx, s.timeout)
		defer cancel()
	}

	ctx, span := s.tracer.Start(
		ctx,
		DirectOrderServiceServiceProviderName+"GetDirectOrder",
	)
	defer span.End()

	// call given implementation of the adapted service provider interface
	getDirectOrderResponse, err := s.grpcClient.GetDirectOrder(ctx, request)
	if err != nil {
		return nil, err
	}

	return getDirectOrderResponse, nil
}

// Close gracefully shuts down the gRPC client connection and releases all associated resources.
// This method should be called when the client is no longer needed to prevent resource leaks.
// It's safe to call Close() multiple times - subsequent calls will be no-ops.
//
// Best Practices:
//   - Always call Close() when done with the client
//   - Use defer client.Close() immediately after successful client creation
//   - Do not use the client after calling Close()
//
// Example:
//
//	client, err := NewDirectOrderServiceGRPCClient(...)
//	if err != nil {
//		return err
//	}
//	defer client.Close() // Ensure cleanup
//
// Returns:
//   - error: Any error that occurred while closing the connection
func (s *directOrderServiceGRPCClient) Close() error {
	if s.conn != nil {
		return s.conn.Close()
	}
	return nil
}

// validateAuth ensures that authentication credentials and group ID are properly configured.
// This method is called during client initialization to prevent runtime authentication failures.
//
// Requirements:
//   - At least one authentication method must be configured
//   - Group ID must be set for all public API calls
//
// Supported Authentication Methods:
//   - API Key: Set via WithAPIKey() option or MESH_API_CREDENTIALS file
//   - Access Token Cookie: Set via WithAccessTokenCookie() option
//
// Returns:
//   - nil: If authentication and group ID are properly configured
//   - error: If authentication method or group ID is missing
func (c *directOrderServiceGRPCClient) validateAuth() error {
	if c.apiKey == "" && c.accessTokenCookie == "" {
		return errors.New("neither api key nor access token cookie set. set credentials via MESH_API_CREDENTIALS file, or use WithAPIKey/WithAccessTokenCookie options")
	}
	if c.groupID == "" {
		return errors.New("group id not set. set via MESH_API_CREDENTIALS file or WithGroup option")
	}
	return nil
}

// authInterceptor creates and returns the appropriate gRPC unary interceptor for authentication.
// This interceptor automatically adds authentication and group ID headers to all outgoing requests
// based on the configured authentication method (API key takes precedence over access token cookie).
//
// Headers Added:
//   - API Key: "Authorization: Bearer <api-key>" header
//   - Access Token Cookie: "Cookie: AccessToken=<token>" header
//   - Group ID: "x-group-id: <group-id>" header (always included)
//
// The interceptor is automatically applied to all method calls and handles the
// authentication and authorization context transparently without requiring manual header management.
//
// Returns:
//   - grpc.UnaryClientInterceptor: Configured authentication and group context interceptor
func (c *directOrderServiceGRPCClient) authInterceptor() grpc.UnaryClientInterceptor {
	if c.apiKey != "" {
		return func(ctx context.Context, method string, req, reply any, cc *grpc.ClientConn, invoker grpc.UnaryInvoker, opts ...grpc.CallOption) error {
			ctx = metadata.AppendToOutgoingContext(
				ctx,
				common.AuthorizationHeaderKey,
				common.BearerPrefix+c.apiKey,
				common.GroupIDHeaderKey,
				c.groupID,
			)
			return invoker(ctx, method, req, reply, cc, opts...)
		}
	}

	return func(ctx context.Context, method string, req, reply any, cc *grpc.ClientConn, invoker grpc.UnaryInvoker, opts ...grpc.CallOption) error {
		ctx = metadata.AppendToOutgoingContext(
			ctx,
			common.CookieHeaderKey,
			common.AccessTokenPrefix+c.accessTokenCookie,
			common.GroupIDHeaderKey,
			c.groupID,
		)
		return invoker(ctx, method, req, reply, cc, opts...)
	}
}
