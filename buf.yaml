version: v2
modules:
  - path: proto
    name: buf.build/mesh/api

deps:
  - buf.build/googleapis/googleapis

# Workspace-level lint and breaking change detection settings.
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE