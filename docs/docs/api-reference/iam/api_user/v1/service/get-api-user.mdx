# GetApiUser

Retrieves a single API user by its unique identifier.

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import serviceData from '../service.generated.json';
import GetApiUserExamples from './GetApiUserExamples';

## Method Details

**Description:** {serviceData.methods.GetApiUser.description}

**Authorization:** {serviceData.methods.GetApiUser.authorization}

**Parameters:** {serviceData.methods.GetApiUser.parameters}

**Returns:** {serviceData.methods.GetApiUser.returns}

**Method Type:** {serviceData.methods.GetApiUser.method_type}

## Code Examples

<Tabs>
<TabItem value="go" label="Go">

```go title="get-api-user.go"
package main

import (
	"context"
	"log"

	api_userv1 "github.com/meshtrade/api/go/iam/api_user/v1"
)

func main() {
	// Create client (loads credentials from MESH_API_CREDENTIALS)
	client, err := api_userv1.NewApiUserServiceGRPCClient()
	if err != nil {
		log.Fatalf("Failed to create client: %v", err)
	}
	defer client.Close()

	// Get API user by name
	apiUser, err := client.GetApiUser(
		context.Background(),
		&api_userv1.GetApiUserRequest{
			Name: "api_users/01HPQR2S3T4U5V6W7X8Y9Z0123", // Replace with actual API user name
		},
	)
	if err != nil {
		log.Fatalf("Failed to get API user: %v", err)
	}

	log.Printf("API User: %s", apiUser.Name)
	log.Printf("Display Name: %s", apiUser.DisplayName)
	log.Printf("Owner: %s", apiUser.Owner)
	log.Printf("State: %v", apiUser.State)
	log.Printf("Roles: %v", apiUser.Roles)
}
```

</TabItem>
<TabItem value="python" label="Python">

```python title="get-api-user.py"
from meshtrade.iam.api_user.v1 import (
    ApiUserServiceGRPCClient,
    ClientOptions,
    GetApiUserRequest,
)

def main():
    # Create client (loads credentials from MESH_API_CREDENTIALS)
    options = ClientOptions()
    client = ApiUserServiceGRPCClient(options)
    
    with client:
        # Get API user by name
        request = GetApiUserRequest(
            name="api_users/01HPQR2S3T4U5V6W7X8Y9Z0123"  # Replace with actual API user name
        )
        
        api_user = client.get_api_user(request)
        
        print(f"API User: {api_user.name}")
        print(f"Display Name: {api_user.display_name}")
        print(f"Owner: {api_user.owner}")
        print(f"State: {api_user.state}")
        print(f"Roles: {api_user.roles}")

if __name__ == "__main__":
    main()
```

</TabItem>
</Tabs>

## Advanced Configuration

For advanced client configuration options (custom endpoints, TLS settings, timeouts), see the [SDK Configuration Guide](../../../../architecture/sdk-configuration).

## Related Methods

- **[ListApiUsers](./list-api-users)** - List all API users in the group
- **[SearchApiUsers](./search-api-users)** - Search API users by criteria
- **[GetApiUserByKeyHash](./get-api-user-by-key-hash)** - Get API user by key hash
