syntax = "proto3";

package feeprofile;

option go_package = "github.com/meshtrade/api/go/instrument/feeprofile";

/*
FeeProfile is the fee profile associated with a particular instrument.
It is used to determine what rate is charged for different things.
*/
message FeeProfile {
  /*
  InstrumentID refers to the instrument against which this fee profile applied.
  */
  string instrumentID = 1;

  string hey =2;
}