# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: permission.proto, role.proto
# plugin: python-betterproto
from dataclasses import dataclass
from typing import List

import betterproto


@dataclass
class Permission(betterproto.Message):
    """Permission is the ability to perform an activity in the system."""

    # ServiceProvider is the name of the Service Provider that provides Service.
    service_provider: str = betterproto.string_field(1)
    # Service is the name of the Service on ServiceProvider that this Permission
    # grants access to.
    service: str = betterproto.string_field(2)
    # Description describes the purpose of this permission.
    description: str = betterproto.string_field(3)


@dataclass
class Role(betterproto.Message):
    """Role is a collection of permissions."""

    # Name is the name of the Role.
    name: str = betterproto.string_field(1)
    # Permissions are the permissions on this role.
    permissions: List["Permission"] = betterproto.message_field(2)
